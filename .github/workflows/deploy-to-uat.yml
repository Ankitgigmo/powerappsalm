name: Deploy Solution to UAT

on:
  push:
    branches:
      - main

jobs:
  deploy-uat:
    runs-on: ubuntu-latest

    steps:
      # STEP 1 — Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # STEP 2 — Install PAC CLI
      - name: Install Power Platform CLI
        uses: microsoft/powerplatform-actions/actions-install@v1
        with:
          pac-version: latest

      # STEP 3 — Pack solution from Git source
      - name: Pack solution
        run: |
          mkdir out
          pac solution pack --folder ./src/MySolution --zipfile ./out/MySolution_Managed.zip
          # ⚠ Replace ./src/MySolution with your actual folder name

      # STEP 4 — Authenticate to UAT
      - name: Authenticate with SP (UAT)
        env:
          TENANT: ${{ secrets.AZURE_TENANT_ID }}
          CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          ENV_URL: ${{ secrets.PP_UAT_URL }}
        run: |
          pac auth create --applicationId $CLIENT_ID --clientSecret $CLIENT_SECRET --tenant $TENANT --environment $ENV_URL --name sp-uat

      # STEP 5 — Import into UAT
      - name: Import solution to UAT
        run: |
          pac solution import --path ./out/MySolution_Managed.zip --overwrite true
